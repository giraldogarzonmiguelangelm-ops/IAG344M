# Clase IA 
## 🤖 Comandos Consola
### ![Python](image-1.png)
### Python
```
python --version <!-- Para saber la version de python -->
python -m venv (nombre+versionpy) <!-- Crea un entorno virtual por primera vez -->
.\env3_14_2\Scripts\activate <!-- Activa el entorno virtual -->
deactivate <!-- Desactiva el entorno virtual -->
pip install pytest  <!-- Instala el test de python -->
python.exe -m pip install --upgrade   <!-- Actualiza versiones -->
pip list <!-- Muestra la lista de librerias -->
set-ExecutionPolicy Unrestricted <!-- Desbloquea la restricción de activar scripts en el panel del pc -->
configure user snippets <!-- Creador de código prediseñado, este se coloca en el panel de comandos -->
```
### Git
![Git](image-2.png)     
```

git --version  <!-- Para saber la versión de git -->
git init <!-- Inicializa el git -->
git add .  <!-- Guarda los datos del archivo en el git  -->
git commit -m "nombre del commit" <!-- Se le asigna un nombre a la caja en donde se guerda el archivo, esto también hace que se cierre la caja -->
```
### Github
![Github](image-3.png)
```
git branch -M main <!-- Crea una nueva rama -->
git remote add origin https://github.com/giraldogarzonmiguelangelm-ops/IAG344.git <!-- Conecta el git con el github, asío todo queda en la nube -->
git push -u origin main <!-- Actualiza los datos del archivo en la nube -->
git clone <!-- Clona dependiendo la ruta que se añada -->
git remote -v <!-- Sale el repositorio en el cual se trabaja -->
```
# Desactivar entorno virtual
```
deactivate

```
# Comandos CMD
|comando|Descripción|
|-|-|
|`cd`|Moverse en directorio|
|`dir`|Listar|
|``|Se sabe en que entorno se esta|


 